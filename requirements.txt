# Instructor / local rebuild (not required for Colab-only students).
# Recreate environment:
#   python3 -m venv gee
#   source gee/bin/activate   # Windows: gee\Scripts\activate
#   pip install -r requirements.txt

earthengine-api>=0.1.400
geemap>=0.30.0
ipywidgets>=8.0.0
jupyterlab>=4.0.0
ipykernel>=6.25.0
matplotlib>=3.7.0
pandas>=2.0.0
numpy>=1.24.0
